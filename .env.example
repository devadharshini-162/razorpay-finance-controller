# No API keys are required for the current deterministic demo.
# The system relies entirely on the deterministic matcher and MockLLMProvider.
#
# To enable real LLM features in a future milestone, you would set your keys here:
# GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_ENABLED=false
GEMINI_MODEL=gemini-1.5-pro